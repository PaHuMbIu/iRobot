{
  "name": "https://LaProfessional.github.io/iRobot",
  "version": "1.0.0",
  "description": "",
  "main": "scripts/header.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite-plugin-static-copy": "^2.3.0"
  },
  "dependencies": {
    "vite": "^6.2.3"
  }
}
